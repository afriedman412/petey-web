# Petey Web TODO

## Features
- [ ] Page-chunked extraction UI: option to split a multi-page PDF into per-page chunks and extract each independently (depends on petey adding `--pages-per-chunk` support)
- [ ] Persistent results storage (currently saves to ephemeral filesystem on Cloud Run)
